val elements: Seq[Int] = Seq(1,2,3,4,5)

println(elements.reverse) // Invert the order of the sequence
println(elements(1)) // Obtain the value of the second position
println(elements.sorted) //Sort all the elements

elements = elements.appended(6)