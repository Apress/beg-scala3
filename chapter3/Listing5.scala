class Engine: //Define the structure of the class
    def start() = print("Start the engine")

val engine = Engine() //Create an instance of the class
engine.start() //Invoke the method to start the engine
